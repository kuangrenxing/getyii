server {
        charset utf-8;
	client_max_body_size 128M;

	listen 80;
	server_name tech_bbs.com www.tech_bbs.com *.dev.www.tech_bbs.com;
	index index.php;
	set $rootdir /app/frontend/web;
	root $rootdir;

	location / {
                try_files $uri $uri /index.php?$args;
	}

	location ~ \.php$ {
                include fastcgi_params;
                fastcgi_index index.php;
                fastcgi_param SCRIPT_FILENAME $rootdir/index.php;
                fastcgi_pass 127.0.0.1:9000;
	}

	location ~ /\.(ht|svn|git) {
                deny all;
	}
}

server {
        charset utf-8;
	client_max_body_size 128M;

	listen 80;
	server_name admin.tech_bbs.com *.dev.admin.tech_bbs.com;
	index index.php;
	set $rootdir /app/backend/web;
	root $rootdir;

	location / {
                try_files $uri $uri /index.php?$args;
	}

	location ~ \.php$ {
                include fastcgi_params;
                fastcgi_index index.php;
                fastcgi_param SCRIPT_FILENAME $rootdir/index.php;
                fastcgi_pass 127.0.0.1:9000;
	}

	location ~ /\.(ht|svn|git) {
                deny all;
	}
}
